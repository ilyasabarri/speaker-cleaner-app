import React from 'react';
import { View } from 'react-native';

// Web stub: react-native-webview is not supported on web
export const WebView = React.forwardRef((props, ref) => <View />);
export default WebView;
